<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rag.auth.mapper.UserMapper">

    <resultMap id="AuthUserResultMap" type="com.rag.common.entity.AuthUser">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status" column="status"/>
        <result property="loginFailCount" column="login_fail_count"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="findById" resultMap="AuthUserResultMap">
        SELECT user_id, username, nickname, password_hash,
               status, login_fail_count, last_login_time, create_time
        FROM sys_user WHERE user_id = #{userId}
    </select>

    <select id="findByUsername" resultMap="AuthUserResultMap">
        SELECT user_id, username, nickname, password_hash,
               status, login_fail_count, last_login_time, create_time
        FROM sys_user WHERE username = #{username}
    </select>

    <select id="existsByUsername" resultType="int">
        SELECT COUNT(1) FROM sys_user WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId" keyColumn="user_id">
        INSERT INTO sys_user (username, nickname, password_hash,
                              status, login_fail_count, last_login_time, create_time)
        VALUES (#{username}, #{nickname}, #{passwordHash},
                #{status}, #{loginFailCount}, #{lastLoginTime}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_user
        SET username = #{username},
            nickname = #{nickname},
            password_hash = #{passwordHash},
            status = #{status},
            login_fail_count = #{loginFailCount},
            last_login_time = #{lastLoginTime}
        WHERE user_id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE sys_user SET status = #{status} WHERE user_id = #{userId}
    </update>

    <update id="updateLoginFailCount">
        UPDATE sys_user
        SET login_fail_count = #{loginFailCount},
            last_login_time = #{lastLoginTime}
        WHERE user_id = #{userId}
    </update>

    <update id="updateLastLoginTime">
        UPDATE sys_user SET last_login_time = #{lastLoginTime} WHERE user_id = #{userId}
    </update>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rag.auth.mapper.KbRolePermissionMapper">

    <resultMap id="KbRolePermissionResultMap" type="com.rag.core.entity.KbRolePermission">
        <id property="id" column="id"/>
        <result property="kbId" column="kb_id"/>
        <result property="roleId" column="role_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="findByKbId" resultMap="KbRolePermissionResultMap">
        SELECT id, kb_id, role_id, create_time
        FROM kb_role_permission WHERE kb_id = #{kbId}
        ORDER BY create_time
    </select>

    <select id="findByKbAndRole" resultMap="KbRolePermissionResultMap">
        SELECT id, kb_id, role_id, create_time
        FROM kb_role_permission WHERE kb_id = #{kbId} AND role_id = #{roleId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO kb_role_permission (kb_id, role_id, create_time)
        VALUES (#{kbId}, #{roleId}, #{createTime})
    </insert>

    <delete id="delete">
        DELETE FROM kb_role_permission WHERE id = #{id}
    </delete>

    <delete id="deleteByKbAndRole">
        DELETE FROM kb_role_permission WHERE kb_id = #{kbId} AND role_id = #{roleId}
    </delete>

</mapper>

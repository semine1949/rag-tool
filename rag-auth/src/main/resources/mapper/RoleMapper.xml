<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rag.auth.mapper.RoleMapper">

    <resultMap id="RoleResultMap" type="com.rag.common.entity.Role">
        <id property="roleId" column="role_id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="findById" resultMap="RoleResultMap">
        SELECT role_id, role_code, role_name, description, status, create_time
        FROM sys_role WHERE role_id = #{roleId}
    </select>

    <select id="findByCode" resultMap="RoleResultMap">
        SELECT role_id, role_code, role_name, description, status, create_time
        FROM sys_role WHERE role_code = #{roleCode}
    </select>

    <select id="findAll" resultMap="RoleResultMap">
        SELECT role_id, role_code, role_name, description, status, create_time
        FROM sys_role ORDER BY role_id
    </select>

    <select id="findByIds" resultMap="RoleResultMap">
        SELECT role_id, role_code, role_name, description, status, create_time
        FROM sys_role
        WHERE role_id IN
        <foreach collection="roleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY role_id
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rag.auth.mapper.DocChunkMapper">

    <resultMap id="DocChunkResultMap" type="com.rag.common.entity.DocChunk">
        <id property="docChunkId" column="doc_chunk_id"/>
        <result property="chunkId" column="chunk_id"/>
        <result property="docId" column="doc_id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="kbId" column="kb_id"/>
        <result property="versionId" column="version_id"/>
        <result property="chunkIndex" column="chunk_index"/>
        <result property="chunkType" column="chunk_type"/>
        <result property="parentChunkId" column="parent_chunk_id"/>
        <result property="content" column="content"/>
        <result property="vectorId" column="vector_id"/>
        <!-- v3 新增：分片策略与参数快照 -->
        <result property="chunkMode" column="chunk_mode"/>
        <result property="paramsSnapshot" column="params_snapshot"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByChunkId" resultMap="DocChunkResultMap">
        SELECT doc_chunk_id, chunk_id, doc_id, tenant_id, kb_id, version_id, chunk_index,
               chunk_type, parent_chunk_id, content, vector_id,
               chunk_mode, params_snapshot, create_time, update_time
        FROM doc_chunk WHERE chunk_id = #{chunkId}
    </select>

    <select id="findByParentChunkId" resultMap="DocChunkResultMap">
        SELECT doc_chunk_id, chunk_id, doc_id, tenant_id, kb_id, version_id, chunk_index,
               chunk_type, parent_chunk_id, content, vector_id,
               chunk_mode, params_snapshot, create_time, update_time
        FROM doc_chunk WHERE parent_chunk_id = #{parentChunkId} ORDER BY chunk_index ASC
    </select>

    <select id="findByDocId" resultMap="DocChunkResultMap">
        SELECT doc_chunk_id, chunk_id, doc_id, tenant_id, kb_id, version_id, chunk_index,
               chunk_type, parent_chunk_id, content, vector_id,
               chunk_mode, params_snapshot, create_time, update_time
        FROM doc_chunk WHERE doc_id = #{docId} ORDER BY chunk_index ASC
    </select>

    <select id="findByDocIdAndVersion" resultMap="DocChunkResultMap">
        SELECT doc_chunk_id, chunk_id, doc_id, tenant_id, kb_id, version_id, chunk_index,
               chunk_type, parent_chunk_id, content, vector_id,
               chunk_mode, params_snapshot, create_time, update_time
        FROM doc_chunk WHERE doc_id = #{docId} AND version_id = #{versionId}
        ORDER BY chunk_index ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="docChunkId" keyColumn="doc_chunk_id">
        INSERT INTO doc_chunk (chunk_id, doc_id, tenant_id, kb_id, version_id, chunk_index,
                               chunk_type, parent_chunk_id, content, vector_id,
                               chunk_mode, params_snapshot, create_time)
        VALUES (#{chunkId}, #{docId}, #{tenantId}, #{kbId}, #{versionId}, #{chunkIndex},
                #{chunkType}, #{parentChunkId}, #{content}, #{vectorId},
                #{chunkMode}, #{paramsSnapshot}, #{createTime})
    </insert>

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="docChunkId" keyColumn="doc_chunk_id">
        INSERT INTO doc_chunk (chunk_id, doc_id, tenant_id, kb_id, version_id, chunk_index,
                               chunk_type, parent_chunk_id, content, vector_id,
                               chunk_mode, params_snapshot, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.chunkId}, #{item.docId}, #{item.tenantId}, #{item.kbId}, #{item.versionId}, #{item.chunkIndex},
             #{item.chunkType}, #{item.parentChunkId}, #{item.content}, #{item.vectorId},
             #{item.chunkMode}, #{item.paramsSnapshot}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="deleteByDocId">
        DELETE FROM doc_chunk WHERE doc_id = #{docId}
    </delete>

    <select id="countByDocId" resultType="int">
        SELECT COUNT(*) FROM doc_chunk WHERE doc_id = #{docId}
    </select>

</mapper>
